class Linked_list():
    pass
def FinderTailvsHead(LinkedList):
    head = LinkedList 
    current = h1

    while current is not None:
        tail = current.next
        current = tail
    return (head, tail)

def sumofLinkedList(l1, l2):
        return (head, tail)

length1 = len(l1)
length2 = len(l2) 
    
if length1 != length2: 
    lowest = l2
    biggest = l1
    if length1 < length2:
        length1, length2 = length2, length1
        biggest, lowest =l2, l1 

     delta = biggest - lowest
     tail = FinderTailVsHead(lowest)[1] 
    s = 0
     while s <= delta:
        new = Linked_list()
        new.val = 0
       tail.


start = 0
while start < len( l1):

     
     
